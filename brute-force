import itertools
numbers = [...]

target_sum = 0

for r in range(1, len(numbers) + 1):
    for combo in itertools.combinations(numbers, r):
        if sum(combo) == target_sum:
            print(combo)
